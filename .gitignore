__pycache__/
*.py[cod]

# Runtime capture output is intentionally repository-local but never source.
/recordings/

# Created by install_linda.sh, in the repo so the systemd unit's ReadWritePaths covers it.
/.venv/
/.venv.backup.*/
/setup.log
